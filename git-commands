git init
ls
ls -a
git config --global user.name "vinodbabu"
git config --global user.email "vinodbabubitra@gmail.com"

git config --list

git config --global --unset user.name
git config --global --unset user.email

git clone https://github.com/vinodbabu29/vinodb.git
ls
cd vinodb/
ls -a
touch file1
ls
git status
git add file1
git status

git commit -m "test commit file1"
git status
git log
git show cid
git push

-----------------
toch file3 file4
ls
git add file3 file4
or
git add .
or 
git add *
or
git add -A
git status
git commit -m "all file"
git status
git log

------------------------
git reset head file-name (staging/index to workspace)
git reset --soft cid (local to staging)
git reset --mixed cid (local to workspace)

git reset --hard cid (to delete commit and commit files)

git commit -m "label" file name (workspace to local only modified files)

rm -rf file-name (to delete file)

----------------------------------
git clone https://github.com/polarapu/batch71.git
ls
cd batch71
touch vinodb
git status
git add vinodb
git status
git commit -m "test commit from vinodb"
git status
git log
git show cid
git push

